{% extends "base.html.twig" %}
{% block body %}
	<main>
		<h1>Les Bonbons</h1>
		<table class='table tble-striped'>
		<tr>
		<th>Nom</th>
		<th>Description</th>
		<th>Date de créa</th>
		<th>Modifier</th></tr>
			{% for bonbon in bonbons %}
				<tr>
					<td>{{bonbon.name}}</td>
					<td>{{bonbon.description}}</td>
					<td>{{bonbon.createAt|date("m/d/y")}}</td>
					<td><a href={{path('admin_article_update',{id:bonbon.id})}}>Modifier</a></td>
				</tr>
			{% endfor %}
		</table>
	</main>
{% endblock %}
